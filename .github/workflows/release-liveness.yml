name: Release Liveness

on:
  pull_request:
    paths:
      - .github/workflows/release-liveness.yml
  schedule:
    - cron: '17 * * * *'
  workflow_dispatch:

permissions: {}

jobs:
  observe:
    permissions:
      actions: read
      contents: read
      pull-requests: read
    uses: RocketsAreNostalgic/.github/.github/workflows/release-liveness-observer.yml@5a90b1ee1537d34b5ad4b3d7f9eb82f08c8faba4
